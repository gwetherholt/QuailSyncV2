/target
.claude/
*.db
certs/
__pycache__/

# Training data and outputs
training/
*.pt

# Runtime data + SQLite sidecars + Pi-agent crash dumps
data/
pi-agent/core.*
*.db-journal
*.db-wal

# Trail-cam secrets — real credentials file must never be committed
# (the .example template is intentionally NOT ignored).
.trailcam-secrets
