services:
  server:
    build: .
    network_mode: host
    volumes:
      - ./data:/data
    restart: always

  sensors:
    build:
      context: ./pi-agent
      dockerfile: Dockerfile.sensors
    network_mode: host
    privileged: true
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - /sys/bus/iio/devices:/sys/bus/iio/devices:ro
    depends_on:
      - server
    restart: always

  camera1:
    build:
      context: ./pi-agent
      dockerfile: Dockerfile.camera
    network_mode: host
    privileged: true
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./pi-agent:/app
      - /usr/lib/python3/dist-packages:/host-packages:ro
      - /usr/lib/aarch64-linux-gnu:/usr/lib/aarch64-linux-gnu:ro
      - /usr/share/libpisp:/usr/share/libpisp:ro
      - /usr/share/libcamera:/usr/share/libcamera:ro
      - /run/udev:/run/udev:ro
    depends_on:
      - server
    command: >
      python3 camera_stream.py
      --server ws://localhost:3000/ws
      --brooder-id 1
      --camera-index 0
      --port 8080
    restart: always

  camera2:
    build:
      context: ./pi-agent
      dockerfile: Dockerfile.camera
    network_mode: host
    privileged: true
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./pi-agent:/app
      - /usr/lib/python3/dist-packages:/host-packages:ro
      - /usr/lib/aarch64-linux-gnu:/usr/lib/aarch64-linux-gnu:ro
      - /usr/share/libpisp:/usr/share/libpisp:ro
      - /usr/share/libcamera:/usr/share/libcamera:ro
      - /run/udev:/run/udev:ro
    depends_on:
      - server
    command: >
      python3 camera_stream.py
      --server ws://localhost:3000/ws
      --brooder-id 2
      --camera-index 1
      --port 8081
    restart: always
