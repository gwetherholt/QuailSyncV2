[pytest]
# Custom markers. Registering them here silences "unknown mark" warnings and
# documents intent.
#   Run everything except the slow real-model tests (good for CI):
#       pytest -m "not integration"
#   Run only the real-model tests locally:
#       pytest -m integration
markers =
    integration: runs real model inference (slow; needs ultralytics + weights)
