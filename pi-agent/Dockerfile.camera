FROM debian:trixie-slim
RUN apt-get update && apt-get install -y python3 python3-pip && rm -rf /var/lib/apt/lists/*
RUN pip install --break-system-packages websockets psutil numpy
WORKDIR /app
COPY camera_stream.py .
